{
  "name": "@sinksight/library-hash",
  "version": "1.0.0",
  "description": "WASM library for computing and verifying SinkSight Library Hashes (SLH)",
  "main": "sinksight_library_hash.js",
  "types": "sinksight_library_hash.d.ts",
  "files": [
    "sinksight_library_hash_bg.wasm",
    "sinksight_library_hash.js",
    "sinksight_library_hash.d.ts"
  ],
  "scripts": {
    "build": "wasm-pack build --target nodejs --out-dir pkg",
    "test": "vitest run",
    "test:fast": "vitest run --exclude tests/hash-golden.test.js"
  },
  "license": "GPL-3.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/crazycat256/sinksight-library-hash"
  },
  "devDependencies": {
    "terser": "^5.46.1",
    "vitest": "^4.1.2"
  }
}
