{
  "name": "@sinksight/library-hash",
  "version": "0.1.0",
  "description": "AST-based JavaScript library fingerprinting (slh1 hashes)",
  "scripts": {
    "build": "wasm-pack build crates/wasm --target nodejs",
    "test": "vitest run",
    "test:fast": "vitest run --exclude tests/hash-golden.test.js"
  },
  "license": "GPL-3.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/crazycat256/sinksight-library-hash"
  },
  "devDependencies": {
    "terser": "^5.46.1",
    "vitest": "^4.1.2"
  }
}
